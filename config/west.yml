manifest:
  defaults:
    revision: main
  remotes:
    - name: zmkfirmware
      url-base: https://github.com/zmkfirmware
    - name: fork
      url-base: https://github.com/rleyvasal
  projects:
    - name: zmk
      remote: fork
      # Official ZMK + patches/zmk-ble.patch (throttle, idle go-dark, adv boost,
      # directed-then-open). Branch: zmk-optimized-484a054. Pin tip SHA.
      # Bumps: .github/workflows/zmk-bump.yml
      revision: 074df9b70720de69c8f961fc8977bd5716adbf03
      import: app/west.yml
  self:
    path: config
