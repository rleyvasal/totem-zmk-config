manifest:
  defaults:
    revision: main
  remotes:
    - name: zmkfirmware
      url-base: https://github.com/zmkfirmware
    - name: fork
      url-base: https://github.com/rleyvasal
  projects:
    - name: zmk
      remote: fork
      # Official ZMK + patches/zmk-ble.patch (throttle, idle go-dark, adv boost,
      # directed-then-open, RPA-aware disconnect + advertising reconcile +
      # bounded recovery reboot). Branch: zmk-optimized-484a054. Pin tip SHA.
      # Bumps: .github/workflows/zmk-bump.yml
      revision: 5606801c95650b5386a444abc75e8260630b13f4
      import: app/west.yml
  self:
    path: config
