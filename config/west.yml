manifest:
  defaults:
    revision: main
  remotes:
    - name: zmkfirmware
      url-base: https://github.com/zmkfirmware
    - name: fork
      url-base: https://github.com/rleyvasal
  projects:
    - name: zmk
      remote: fork
      repo-path: zmk-next
      # Runtime Config plus the validated Totem BLE reconnect patch. Keep this
      # exact revision pinned so production and Studio test artifacts use the
      # same protocol and persistence implementation.
      revision: bdcf24d0d65a9f21b14a1da63cae1a260913ab43
      import: app/west.yml
  self:
    path: config
